[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "orx"
version = "0.1.0"
description = "ORX control plane scaffold"
requires-python = ">=3.11"

[project.scripts]
orx = "orx.cli:main"

[tool.setuptools]
packages = ["orx"]
